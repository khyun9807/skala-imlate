# 프론트엔드 환경변수 예시 파일.
# 실제 사용 시 `.env.local` 로 복사해서 값을 채운다. (`.env.local` 은 커밋하지 않는다)

# API 베이스 경로.
# - 개발: 비워 두면 기본값 `/api/v1` 이 사용되고, vite dev 서버가 http://localhost:8080 으로 프록시한다.
# - 배포(프론트/백엔드 도메인이 다를 때): 예) https://imlate.example.com/api/v1
VITE_API_BASE=/api/v1
